# Release v1.11.8 (Build 56)

**Data**: 2026-04-19
**versionName**: 1.11.8
**versionCode**: 56 (Android)

---

## Resumo da Release

Duas correcoes para a atualizacao de metadados (titulo, artista, artwork) ao mudar de musica em background:

1. **In-app artwork**: React `key` em vez de `recyclingKey` no expo-image — forca unmount/remount do componente Image, contornando o lifecycle do Glide que nao recarrega apos background
2. **Lock screen metadata**: Bug encontrado no AudioControlsService nativo — quando duas musicas usam a mesma artworkUrl (fallback logo), `loadArtworkFromUrl` pula o download E a reconstrucao da notificacao. Fix: passar `undefined` como artworkUrl quando nao ha artwork cacheado, forcando o branch `?: postOrStartForegroundNotification()` que sempre reconstroi a notificacao

---

## Causa Raiz

O expo-image usa **Glide** como biblioteca de imagens no Android. Quando o app vai para background:

1. Glide **pausa** todos os requests de imagem (vinculado ao lifecycle da Activity)
2. No `onResume`, Glide retoma requests pendentes mas **NAO recarrega** imagens que ja foram loaded
3. O metodo `rerenderIfNeeded()` compara `sourceToLoad != loadedSource` — se a view nativa tem a imagem antiga carregada, o Glide ve `loadedSource` preenchido e nao recarrega mesmo com nova source

Referencia: [expo/expo #22100](https://github.com/expo/expo/issues/22100)

### Por que `recyclingKey` (v1.11.7) falhou

`recyclingKey` opera no nivel nativo do expo-image: seta `clearViewBeforeChangingSource = true` e chama `rerenderIfNeeded()`. Mas como o Glide esta em estado pausado/resumido apos background, a chamada pode ser ignorada. O `recyclingKey` foi projetado para FlashList recycling, NAO para transicoes de lifecycle.

### Por que `key` (React) funciona

O `key` opera no nivel do **React**, nao do modulo nativo:
1. React **destroi** o componente Image antigo (native view destruida pelo React reconciler)
2. React **cria** uma nova instancia do componente Image
3. Nova `ExpoImageViewWrapper` → `loadedSource = null` → Glide faz load **obrigatorio**

Validado pelo projeto web irmao (`olha-que-duas`), que usa `key={layerSrc}` no `<img>` para forcas remount no componente `ArtCrossfade`.

---

## Correcoes

### 1. NowPlaying.tsx — `key` em vez de `recyclingKey`

```tsx
// ANTES (v1.11.7):
<Image recyclingKey={musicRecyclingKey} source={{ uri: musicArtUri }} ... />

// DEPOIS (v1.11.8):
const songKey = `${nowPlaying.song.title}\0${nowPlaying.song.artist}`;
<Image key={songKey} source={{ uri: musicArtUri }} ... />
```

O `key` e baseado na **identidade da musica** (titulo + artista), nao na URI. Isto evita duplo remount quando `localArtUri` chega para a mesma musica (transicao remote → file://).

Aplicado nos tres modos com artwork: `music`, `podcast`, `announcement`.

### 2. radioService.ts — artworkUrl undefined em vez de fallbackLogo

**Problema no nativo**: `AudioControlsService.loadArtworkFromUrl()` compara `url != currentArtworkUrl`. Se duas musicas consecutivas nao tem artwork cacheado, ambas usavam `fallbackLogo` como artworkUrl. A funcao detetava a mesma URL e pulava tudo — incluindo `postOrStartForegroundNotification()`. Resultado: `currentMetadata` tinha os dados novos mas a notificacao nunca era reconstruida.

**Fix**: `pickArt()` agora retorna `data.localArtUri || undefined` (em vez de `|| fallbackLogo`). Quando `artworkUrl` e undefined/null no lado Kotlin, o Elvis operator `?:` dispara `postOrStartForegroundNotification()` diretamente, reconstruindo a notificacao com os novos titulo/artista e mantendo o `currentArtwork` anterior (logo ou artwork da musica anterior).

A primeira chamada (`setActiveForLockScreen`) continua a passar o logo URL para popular `currentArtwork` como bitmap de fallback.

### 3. useNowPlaying.ts — AppState listener (mantido da v1.11.7)

O listener de AppState que forca `setData({ ...current })` ao regressar do foreground continua a garantir que o React re-renderiza o NowPlaying com dados frescos e nova referencia de objeto.

---

## Comportamento por Cenario

| Cenario | key muda? | Resultado |
|---|---|---|
| Musica diferente em background | Sim | React unmount/remount → Glide load novo |
| 3+ musicas em background | Sim (ultimo songKey) | Mostra musica actual |
| Mesma musica, URI remote→file:// | Nao | expo-image atualiza source in-place |
| Mesma musica repetida | Nao | Sem flash desnecessario |
| localArtUri null (download falhou) | Sim (nova musica) | Usa remote URL (prefetched) |

---

## Arquivos Modificados

- `src/components/radio/NowPlaying.tsx` — `recyclingKey` substituido por React `key` em todos os Image
- `src/services/radioService.ts` — `pickArt()` retorna undefined em vez de fallbackLogo; `updateLockScreen` aceita artworkUrl opcional; primeira chamada sempre inclui logo
- `app.json` — version 1.11.7 → 1.11.8, versionCode 55 → 56
- `package.json` — version 1.11.7 → 1.11.8
- `CLAUDE.md` — versoes atualizadas
- `docs/releases/v1.11.8.md` — este documento

---

## Textos para Google Play Store

### Notas de Versao (PT) - "What's New" (max 500 chars)
```
v1.11.8 - Artwork corrigida

Correcoes:
- A foto da musica agora atualiza imediatamente quando volta ao app apos ouvir em background
- Corrigido problema do Android onde a imagem ficava presa na musica anterior
```

### Notas de Versao (EN) - "What's New" (max 500 chars)
```
v1.11.8 - Artwork fix

Fixes:
- Album artwork now updates immediately when returning to the app after background listening
- Fixed Android issue where the image would stay stuck on the previous song
```
